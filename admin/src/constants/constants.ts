const url = "http://localhost:8080/api/"
const PAGE_SIZE = 5;
const ROLE = {
    CUSTOMER: "customer",
    STAFF: "staff"
}
const GENDER = {
    MALE: "male",
    FEMALE: "female",
    OTHER: "other"
}

const constants = {
    url,
    PAGE_SIZE,
    ROLE,
    GENDER
}

export default constants